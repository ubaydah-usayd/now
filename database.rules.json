{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "projects": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "tasks": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "sessions": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "dailyLogs": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "appState": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}